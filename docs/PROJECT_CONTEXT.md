# Project context — Astra Street

The user resumed this existing project on the MacBook on 2026-09-06. Latest source was pulled from the private Sites repository, advancing `main` from `0269613` to `a711012`. No GitHub project was merged. The user then shared https://x.com/higgsfield_ai/status/2095916820431827408 and explicitly chose to move toward street-racing gameplay.

The active brief is a visually rich browser arcade street racer: an original sports coupe, canyon/forest/city routes, handbrake drifting, nitro, score chains, and simple fun gameplay. The older F1/AAA brief is historical. The current work is an initial interpretation of the new video; it does not claim parity.

Preserve the Sites/Vinext/Three.js checkout, clear modular code, local personal Git identity, frame/pixel caps, suspension when hidden, input clearing and owned-resource disposal. Aim for 60 FPS; 40–50 is acceptable while other GPU work runs. Do not infer thermals or quiet operation from FPS. Do not use Orca. Any explicitly authorized asset worker must be GPT Astra. The Sites skill authorized one bounded image-generation worker for this pass; the owner integrated all source and assets.

Reuse project `appgprj_6a9c96484ab081919378a4aa6684a3f3`. Source is in its private Git repository. Public GitHub publication and a repository-wide source license remain pending. Current evidence, publishing status and next tasks are in `HANDOFF.md`.

The latest user feedback requests a large landscape/detail increase and corrected horizontal steering. The current pass adds dense forests, solid roadside conifers, sculpted hills, fractured canyon walls and distant mountain relief. Browser automation must use a separate headless profile off the user’s screen. The personal GitHub copy is `rcbran/astra-street`, private by default; `origin` and `sites` serve GitHub and Site source respectively. See `HANDOFF.md` for final release status.
