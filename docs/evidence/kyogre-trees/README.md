# Kyogre tree correction evidence — 2026-09-06

Dedicated headless Chromium 153.0.8010.12 on Arch Linux / Radeon 780M (ANGLE, Mesa radeonsi phoenix ACO). Other projects/agents may use the machine. No visible browser, isolated-GPU, thermal, power, human-handling or physical-device claims.

- `broadleaf-worlds.json`: six frozen Pinecrest/Canyon × clear/sunset/rain texture comparisons through the normal presentation pipeline. PNG before/after is the exact same Pinecrest clear scene. The 1440×900 CSS/DPR2 screenshot is larger than its actual 1675×1047 framebuffer; other comparison framebuffers are recorded individually.
- `broadleaf-angles.json`, `broadleaf-near.png`, `broadleaf-far.png`: 62 broadleaf views over three LODs. Far coverage remains about 57% of near; this is descriptive static coverage, not visual equivalence or FPS.
- `browser-check.json`, `touch-check.json`, `render-check.json`: production controls/responsive UI, emulated multi-touch, DPR/quality/fullscreen, camera, world cycles, shared-asset lifetime and presentation-target checks. Renderer counts are not JavaScript heap measurements.
- `full-races.json`, `summary.json`: nine complete two-lap production races and 27 world-cycle rows, using the first padded atlas (`28bbaeba…`). Eight medians round to 30 FPS; wet Pinecrest is 26.56 FPS median / 22.86 FPS tenth percentile. Raw snapshots record actual framebuffer dimensions about once per second. Initial freeze assertions cover player distance, clock and finishing position.
- `race-completion-audit.json`: offline corroboration of two full laps in all nine original runs, using recorded final distances and the unchanged route code.
- `final-wet-pinecrest.json`: focused production repeat with the final atlas; 22.62 FPS median / 18.69 FPS tenth percentile. Stronger two-lap distance and all-driver/result freeze checks, three world cycles and browser-error checks passed. Shared load was not isolated, so the timing difference from the earlier sweep is not assigned to the atlas change.
- `presentation-cost.json`: fixed Pinecrest camera/geometry at 1821×1138. The cost difference includes the MSAA4 half-float presentation and fullscreen passes, not just the shading shader. Twenty retained asynchronous GPU samples per mode, after four warm-up pairs, alternating order. Direct rendering retains the same Balanced world and shadows; it bypasses only the contact-shading presentation pipeline. This is a cost experiment, not race FPS.

The asset correction changes only broadleaf atlas RGB outside protected UVs. `../../assets/tree_small_02-padding.json` records exact hashes and byte-preservation invariants. Final visual reports use `2581f473…`, with the conservative two-texel margin and independent continuous bilinear-support check. Nine-race timing and earlier UI/resource/presentation-cost suites used the first padded atlas `28bbaeba…`; the final correction changes only RGB while preserving geometry, alpha, materials and the runtime pipeline. Other model assets match the MacBook checkpoint. Reports identify dirty source based on `8d30b17`; that parent SHA alone does not identify the measured asset correction. All gameplay assets remain local.

Full screenshots and regenerated comparisons stay ignored under `artifacts/`. The portable report/capture scripts are in `scripts/`; see `../../DEVELOPMENT.md`.
